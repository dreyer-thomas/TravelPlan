---
topic: TravelPlan UI/UX redesign review
updated: 2026-07-30T23:14
---

- (assumption) Source scan confirmed: ux-design-specification.md (2026-02-12), ux-design-directions.html, architecture.md, prd.md used as sources for this UX pass.
- (event) Extraction subagent found major drift between the 2026-02-12 UX spec's chosen visual direction (teal/sand/coral palette, Source Sans 3 + Fraunces typography, custom 320/768/1024 breakpoints, Navigation Rail IA) and the live implementation (orange/navy MUI theme, default system fonts, stock MUI breakpoints).
- (assumption) Implementation shows ad hoc styling drift: hardcoded hex colors in sx props diverging from theme tokens, two competing card idioms (elevation-1 vs elevation-0+manual-border), inconsistent borderRadius values (1 to 3 plus px strings) despite theme.shape.borderRadius=12, and two map components using different marker colors for the same concept.
- (assumption) PRD edit history shows accessibility target was downgraded (WCAG AA removed) after the UX spec was written; the UX spec still states AA contrast + full keyboard nav as goals -- docs are no longer aligned.
- (assumption) TripDayView.tsx (2278 lines) and TripTimeline.tsx (771 lines) are large components mixing many concerns; likely structural driver of the per-section style drift, since each block is styled locally instead of via shared primitives.
- (event) Generated 4 contrasting visual-direction mockups for the Trip Overview hero screen (.working/direction-{calm-trustworthy,warm-adventurous,minimal-modern,bold-datarich}.html) to elicit Tommy's reaction, since he could name dissatisfaction but not a direction.
- (decision) Tommy's reaction to the 4 direction mockups: likes 'Reise-Cockpit' (bold-datarich) structure/framing/density but wants LIGHT mode not dark. Wants merged direction: colors from 'Ruhiger Reisebegleiter' (calm-trustworthy), layout/borders/data-viz structure from 'Reise-Cockpit' (bold-datarich), sans-serif typography (no serif headings from direction 1). Also wants a Day-Detail screen mocked, not just Trip Overview.
- (event) Produced merged 'light cockpit' direction (.working/direction-merged-light-cockpit.html): calm-trustworthy palette (paper #F7F4EC/#EFEAE0, ink #2B2A26/#6B675C, forest-green accent #4B6358 + sage #7C9483, terracotta #8A5A2B/#F6ECE0 for gap/warn) on bold-datarich's structural DNA (stat-strip, bordered cards, gradient budget bar), pure sans-serif. Covers both Trip Overview and a new Day-Detail screen (previous-night checkout, day items w/ time tags + travel segments, current-night check-in, day budget sidebar).
- (decision) Merged 'light cockpit' v1 read as too plain/low-accent. Tommy wants photography as the missing accent: Trip Overview hero should use a full photo header (like warm-adventurous direction 2) with UI elements placed over/on the photo instead of a flat gradient. Day Detail screen should get a matching day-hero photo at its top (same treatment as trip hero, for visual consistency) plus small thumbnail images per activity/day-item in the timeline.
- (event) Produced v2 (.working/direction-merged-light-cockpit-v2-photos.html): added photo-hero header (CSS-gradient stand-in with dark scrim + overlaid title/date/stat-pills) to both Trip Overview and Day Detail screens for visual consistency, plus small thumbnail images per timeline activity/travel-segment. Color system/typography from v1 unchanged, no new accent colors introduced.
- (decision) v2 photo-hero direction approved as 'quite good' overall. Refinements requested for v3: (1) remove thumbnail images from travel/drive segments between activities -- keep those text/icon+duration only; (2) replace car emoji with a cleaner, more modern line-style car icon; (3) day/activity items usually have MULTIPLE photos in reality -- show them as an image strip/row at the BOTTOM of the activity tile, not a single inline thumbnail; (4) images should have square/sharp corners, no border-radius on the photos themselves (cards can keep their own radius).
- (event) Produced v3 (.working/direction-merged-light-cockpit-v3.html): removed travel-segment thumbnails (text+icon only), replaced car emoji with an inline SVG line-icon car, restructured activity/accommodation tiles to show a bottom photo strip (2-3 images) instead of one inline thumbnail, set border-radius:0 on all photo elements while keeping card radii unchanged.
- (decision) v3 photo-strip refinement for v4: thumbnails within an activity's photo strip must all be the same uniform size, left-aligned (not stretched/justified across the tile width), square aspect ratio, center-cropped (object-fit: cover, object-position: center).
- (event) Produced v4 (.working/direction-merged-light-cockpit-v4.html): photo-strip thumbnails now uniform 56x56px squares, left-aligned via flex:0 0 56px + justify-content:flex-start (no longer stretched), center-crop via background-position:center, border-radius:0 unchanged.
- (decision) v5 refinement for Trip Overview (Screen A) day-row list: add a square, center-cropped hero/thumbnail image per day row, placed where the date/day-number currently sits (leftmost position). Date and day-number move to a different position within the row (agent's judgment, consistent with existing patterns), e.g. as a compact caption near the day title.
- (event) Produced v5 (.working/direction-merged-light-cockpit-v5.html): Screen A day-rows now show a 72x72 square center-cropped day-photo in the leftmost slot (was date/day-number column); date+day-number relocated to a compact caption ('Tag 01 · 12.9') above the day title. Screen B untouched (verified byte-identical via diff).
- (decision) Day-detail timeline (Screen B) keeps the vertical line/dot structure. Per-activity emoji icons (car/plate/wine-glass) are REMOVED and replaced with one neutral, uniform dot marker for all generic day-plan-item activity nodes -- there is no 'activity type' field in the data model and Tommy does not want to require picking a category/icon per activity. Accommodation ('stay') dots keep their house icon (derived from item type, no new input needed) and travel-segment dots keep the car SVG (derived from existing transportType field, no new input needed). This constraint (no per-activity type field, neutral marker only) must carry into EXPERIENCE.md's component pattern for the day timeline.
- (event) Produced v6 (.working/direction-merged-light-cockpit-v6.html): all 3 .tl-dot.activity emoji replaced with one identical neutral 8px solid dot marker (::after on existing 32px .tl-dot circle). Stay/travel-segment icons and Screen A untouched (diff-verified).
- (decision) v7 additions: (1) Re-introduce the day-coverage Gantt-style bar (planned vs. open time blocks -- accommodation/activity/travel segments vs gaps), matching the existing production TripDayGanttBar concept. Placement: a full-width coverage bar under the hero photo on Screen B (Day Detail), PLUS a compact mini version per day-row on Screen A (Trip Overview) so gaps are visible at a glance in the list too. (2) Simplify the budget/cost display in the mockups to a simple 'Kosten bisher: EUR X' running total instead of a planning/breakdown-oriented budget panel -- this is a visual-emphasis simplification only for this design pass, NOT a product decision to remove the existing payment-schedule/monthly-view feature (stories 4.5/4.6 stay as-is in the backlog; flagged for John/PM only if Tommy later wants the feature itself reduced).
- (decision) v8: On Screen B (Day Detail), the hero-photo stat pills (check-in time, cost, drive time) duplicate the stat-strip/coverage panel now shown directly below the hero -- same numbers appear twice stacked. Remove the stat pills from the day hero photo entirely; hero photo keeps only title/date/back-button. Also check Screen A's hero for the same duplication against its stat-strip and flag/fix if present.
- (event) Produced v7 (.working/direction-merged-light-cockpit-v7.html): re-added day-coverage Gantt bar (full panel on Screen B below hero, mini bar per day-row on Screen A, Porto gap-day shows large open segment), simplified budget panels on both screens to a plain 'Kosten bisher' running total + itemized list (payment-schedule/monthly styling removed).
- (event) Produced v8 (.working/direction-merged-light-cockpit-v8.html): removed duplicated hero-photo stat-pills on BOTH screens (Screen B: Fahrzeit/Ausgaben/Check-in; Screen A: Dauer/Kosten bisher/Offene Punkte) since they repeated the stat-strip/coverage panel directly below. Hero photos now show only title/date/navigation.
- (decision) v9: (1) Replace hero-photo CSS-gradient placeholders on both screens with a REAL photo (travelplan/public/hero-mountains.jpg, an existing demo asset already shipped with the product, 3994x2246) to stress-test the scrim/crop/text-legibility technique with real imagery instead of a gradient stand-in. (2) Bucket List and Day-Map panels (real existing features: Story 3.5 Day View Map Panel, Story 4.1/4.2 Trip Bucket List) were out of scope for the hero-screen mockups so far and are missing -- add both to Screen B (Day Detail), map panel above bucket-list panel below it (matching real app AC in Story 4.2), using the established v8 design tokens/card conventions, no new colors.
- (event) Produced v9 (.working/direction-merged-light-cockpit-v9.html): swapped real hero-mountains.jpg photo into both hero headers (visually verified via screenshot). Crop keeps peak+tree line in frame, trims some sky/lake detail. Bottom title text stays well legible on existing scrim; top breadcrumb text over sunlit rock had thin contrast margin -- fixed by adding a light 4th scrim stop at the top (0 -> 0.26 opacity) so all photos get baseline protection there. Added Day-Map card (reusing Screen A's map-preview pin technique) and Bucket-List card (4 example idea places w/ + affordance) to Screen B sidebar, below the existing day overview card.
- (decision) Product-scope decision (out of UX lane, flagged for John/PM after this design pass finishes): Tommy wants comments AND voting removed from the product entirely (Epic 5 stories 5.3/5.5/5.7, Epic 6 stories 6.1-6.3, all currently 'done'). Mockups already correctly omit any comments/voting UI. Sequencing: finish current DESIGN.md/EXPERIENCE.md pass first, then bring John in to process the removal as a backlog change.
- (decision) v10: The 'Heute im Ueberblick' card on Screen B (Day Detail) sidebar mostly duplicates info already shown in the hero/title area above it. Remove that card entirely.
- (event) Produced v10 (.working/direction-merged-light-cockpit-v10.html): removed the redundant 'Heute im Ueberblick' sidebar card (Fahrsegmente/Fahrzeit gesamt/Aktivitaeten counts) since Fahrzeit gesamt literally duplicated the stat-strip and the other two were derivable from the coverage bar/timeline already on screen. Sidebar now flows Kosten bisher -> Tageskarte -> Bucket-Liste with no gap.
- (decision) Tommy approved v10 as the final direction ('genau so umsetzen'). Moving into Finalize: opted IN to an accessibility reviewer lens, and requested 3 additional key-screen mockups before closing (Trips-Liste, Share-Dialog, Login/Register) using v10's established tokens, since only Trip Overview + Day Detail were mocked so far.
- (event) Produced 3 additional key-screen mockups applying the approved v10 design system (.working/additional-screens-trips-list-share-login.html): Trips-Liste (trip-row list w/ square thumbnails + status pills + Neue Reise CTA), Share-Dialog (reskinned MUI dialog w/ role badges from existing tokens, email+role only per real data model), Login/Register (side-by-side auth cards w/ hero-photo side panel).
- (decision) Rubric-walker review found the entire authoring/CRUD half of the product (create trip, add accommodation/day-item, password reset, generic form primitives) unmocked. Tommy chose to close this gap now rather than defer: add mockups for Trip-Create dialog, Add-Accommodation/Day-Item form dialog, and Password-Reset flow, using the same established v10 design system tokens.
- (event) Produced .working/additional-screens-forms-authoring.html: Trip-erstellen modal, Eintrag-hinzufuegen modal (Unterkunft/Aktivitaet tabs, photo drag-drop), Passwort-zuruecksetzen 2-step flow, and a Form-Bausteine component swatch sheet. All interactive elements bumped to >=44px touch target per the accessibility review (buttons/inputs/selects/tabs min-height 44px, photo-add-tile 56px replacing the flagged 24px bucket-add pattern, new 44px-tall checkbox-row primitive).
- (event) spines finalized
- (event) Applied structural editorial review (condensed 2 restated bullets in DESIGN.md, merged duplicate gap-state row in EXPERIENCE.md). Resolved the flagged [QUESTION FOR UX] on trip-status pill count myself by checking mockups/trips-list-share-login.html directly: confirmed 4 real states (gap/vollstaendig/upcoming/past) via CSS classes .trip-status.gap/.upcoming/.past + default -- fixed the badge/pill component row in DESIGN.md accordingly.
- (event) Applied prose editorial review (6 grammar/agreement fixes). Resolved both remaining prose-review queries myself: 'five surfaces' corrected to 'nine' (matches the full IA table A-I); 'N Tage offen' fixed-plural template corrected to proper German singular/plural agreement ('1 Tag offen' for N=1) in DESIGN.md and both EXPERIENCE.md occurrences, matching the doc's own existing usage examples.
- (event) UX design pass for TravelPlan CLOSED. Final artifacts: DESIGN.md, EXPERIENCE.md (status: final), 3 mockups/ files, review-rubric.md, review-accessibility.md, validation-report.md/html, review-editorial-structure.md, review-editorial-prose.md. Next: hand off comments/voting removal (product-scope decision, out of UX lane) to John/PM.
